# import the libraries

import unittest

# to test if only integar number within a list that contains both float and integars values are selected and added

class test_range (unittest.TestCase):
    def testing(self):
        input
